//remove first value in linked list 
public E remove()
{
	if(head==null)
		return null;
	
	//hold to return removed
	E temp = head.element;
	head = head.next;
	if(head==null)
		tail=null;
	return temp;
	
}